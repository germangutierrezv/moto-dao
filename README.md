# Moto DAO building with just Javascript

### **Welcome 👋**
To get started with this project:

1. Run `npm install` at the root of your directory
2. Run `npm start` to start the project
3. Start coding!